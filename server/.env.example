# Server API Base URL (called by the client)
PORT=5000

# LLM Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Accessibility Audit
AUDIT_TIMEOUT_MS=60000

# CORS — set to your client origin in production
CORS_ORIGIN=http://localhost:3000
